# Orchestrator E2E Test Configuration
# Copy this to .env.test and fill in your values

# Required: Your Arke API key (user key, not agent key)
ARKE_API_KEY=uk_your_api_key_here

# Optional: Override API URL (default: https://arke-v1.arke.institute)
# ARKE_API_URL=https://arke-v1.arke.institute

# Optional: Override agent endpoint (default: from agent.json)
# AGENT_ENDPOINT=https://your-orchestrator.workers.dev

# Optional: Override agent ID (default: from agent.json)
# AGENT_ID=01ABC...
